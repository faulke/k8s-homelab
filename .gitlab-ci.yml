deploy:
  image:
    name: bitnami/kubectl:latest
    entrypoint: ['']
  script:
    - kubectl config get-contexts
    - kubectl config use-context faulke/homelab:homelab-agent-dev
    - kubectl get pods --all-namespaces